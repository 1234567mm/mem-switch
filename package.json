{
  "name": "mem-switch-desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "prepare-sidecar": "python scripts/prepare-sidecar.py",
    "tauri-build": "npm run prepare-sidecar && tauri build"
  },
  "dependencies": {
    "@tauri-apps/cli": "^2.10.1"
  }
}
